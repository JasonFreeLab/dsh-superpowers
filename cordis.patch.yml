# dsh-superpowers bundle patch.
# Install with `dsh plugin --profile <name> add @jasonfreelab/dsh-superpowers`; this patch is
# then merged into the profile's composition to inject the superpowers skill provider.
- insert:
    - id: superpowers
      name: "@jasonfreelab/dsh-superpowers"
